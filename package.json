{
  "name": "gesthero",
  "version": "1.1.0",
  "private": true,
  "description": "Lightweight mouse gesture controls for Chromium browsers.",
  "scripts": {
    "test": "node --test",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{js,json,html,md}\"",
    "format:check": "prettier --check \"**/*.{js,json,html,md}\"",
    "build": "python3 build_extension.py"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  },
  "license": "MIT"
}
